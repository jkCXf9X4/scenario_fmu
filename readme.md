# Scenario FMU

## Note




# Build

Uses vcpkg for dependencies

```
git submodule init && git submodule update

cmake --preset=set1

cmake --build build
cmake --build build &> build.log
```


# Credits
